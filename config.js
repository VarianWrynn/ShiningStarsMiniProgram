
const config = {
  /*const是ES6新增的特性，如果我们声明一个固定不变的常量，这应该用cost关键字
  const一旦被设置，其他地方是无法更改这些配置，尝试修改则会报错*/
  
  //api_base_url:"https://www.tmclee.com/"
  
  
  api_base_url: "https://localhost:9527/"
}

export {config}